<li>
	{{burger_name}}

		{{#if ordered1}}
	<button class="change-devoured" data-id="{{id}}" data-devoured="{{ordered1}}">
		Devour TIME!
	</button>
		{{/if}}


<button class="delete-devoured" data-id="{{this.id}}">DELETE!</button>
</li>

